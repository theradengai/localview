# Prototype Reference

The original interactive prototype defines the visual target.

Keep these elements:

- macOS title bar
- left file tree
- editor/preview workspace
- Edit / Split / Preview modes
- status bar
- minimal black and white aesthetic

The production app should preserve this interaction model while replacing mock data with the real filesystem.

